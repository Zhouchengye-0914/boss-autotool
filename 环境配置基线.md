# BOSS 直聘自动化助手环境配置基线

最后核验时间：2026-07-20（Asia/Shanghai）

## 固定运行环境

| 项目 | 固定值 |
|---|---|
| 项目目录 | `D:\projects\boss-job-assistant` |
| Python | `3.10.1` |
| Python 路径 | `D:\Python3.10.1\python.exe` |
| DrissionPage | `4.1.1.4` |
| PyYAML | `6.0.2` |
| pytest | `9.1.1`（项目测试使用标准库 unittest） |
| pydantic | `2.13.4`（项目不依赖） |
| Chrome | `150.0.7871.127` |
| Chrome 路径 | `C:\Program Files\Google\Chrome\Application\chrome.exe` |

完整 Python 包快照见 `environment-freeze.txt`，用于审计，不作为其他项目新增包的启动门禁。

## 硬性规则

- 不运行 `pip install`、`pip uninstall`、升级或降级命令。
- 不修改共享环境 `D:\projects\.venv`。
- 不创建需要重新安装包的新虚拟环境。
- 所有命令显式使用 `D:\Python3.10.1\python.exe`。
- 真实运行前校验 Python 3.10.1、DrissionPage 4.1.1.4、PyYAML 6.0.2；只有本项目依赖不一致时才拒绝启动 Chrome。
- 其他项目新增或移除的无关包只输出审计提示，不阻止本项目运行。
- Chrome 使用项目专用用户目录，不读取系统默认 Chrome 用户配置。

## 常用命令

```powershell
cd D:\projects\boss-job-assistant
D:\Python3.10.1\python.exe .\启动.py
D:\Python3.10.1\python.exe .\启动.py --dry-run
D:\Python3.10.1\python.exe .\启动.py chat-check
D:\Python3.10.1\python.exe .\启动.py run --tasks .\data\tasks.example.json
D:\Python3.10.1\python.exe main.py validate
D:\Python3.10.1\python.exe main.py --dry-run
D:\Python3.10.1\python.exe main.py environment-check
D:\Python3.10.1\python.exe main.py browser-check
D:\Python3.10.1\python.exe main.py run --tasks .\data\tasks.example.json
D:\Python3.10.1\python.exe -m unittest discover -s tests -v
```

`browser-check` 只检查 Chrome 和手动登录。`run` 会执行真实页面操作，必须换成经过核验的真实任务文件。
